Де що знаходиться:

-  screens - скріни =)
- `agent/` — інструкції для створення Jenkins Worker (агента)
- `jenkins/` — інструкції для створення Jenkins Master
- `devops_soft/forStep2/` — основна директорія з застосунком та CI/CD пайплайном:
  - `app/` — вихідний код Node.js застосунку з тестами
  - `Dockerfile` — збирає імедж застосунку
  - `Jenkinsfile` — автоматизований CI/CD процес
